#!/usr/bin/env bash
# Switch foot terminal to light theme using SIGUSR2 signal
# Targets foot server process to update all clients instantly

# Send SIGUSR2 to foot server to switch to [colors2] section (light theme)
# Run in background subshell to avoid signal propagation issues with darkman
(pgrep -x foot | xargs -r kill -SIGUSR2) &
exit 0
